var primeiro = [prompt("Manda a boa ai, texto pls")];
var segundo = [parseInt(prompt("Insira um numero agora"))];

function receive(a,b) {
    return array = [a, b];
};

console.log(receive(...primeiro, ...segundo));